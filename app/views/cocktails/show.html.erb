<!-- <h1>Show : <%= @cocktail.name %></h1>
  <%= @cocktail.doses.count %>
  <ul>
    <% @cocktail.doses.each do |dose| %>
      <li><%= dose.ingredient.name %> : <%= dose.description %><%= link_to "Delete", dose_path(dose), method: :delete, data: { confirm: 'Are you sure?' } %></li>
    <% end %>
  </ul> -->

<h1><%= @cocktail.name %></h1>

<ul>
    <% @cocktail.doses.each do |dose| %>
      <li>
        <%= dose.ingredient.name %> (<%= dose.description %>)
        <%= link_to "supprimer", dose_path(dose), method: :delete, data: {confirm: "Etes vous certain?"} %>
      </li>
    <% end %>

</ul>

<hr>
<%= link_to "ajouter un ingrédient", new_cocktail_dose_path(@cocktail) %>


<h2>Add image :</h2>
<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient, collection: Ingredient.all %>
  <%= f.input :description %>
  <%= f.submit "Add ingredient", class: "btn btn-primary" %>
<% end %>
